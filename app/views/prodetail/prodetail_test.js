'use strict';

describe('myApp.prodetail module', function() {

  beforeEach(module('myApp.prodetail'));

  describe('prodetail controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});